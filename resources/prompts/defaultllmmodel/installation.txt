We have a code repository with the following file structure:

{files_summaries}

The URL for the remote repository is: {repository_url}

Write the text for the Installation section of this repository's README. The Installation section must contain (as subsections) the list of prerequisites, the list of the featured used packages,  and environment setup instructions (paying attention to complete installation commands, including the repository cloning). All commands shoudl be supplied in separated text blocks instead of inlines. Add the title (top level) to the text. The output must be a markdown text.